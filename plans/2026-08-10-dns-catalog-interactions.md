# DNS-like catalog interactions

## Цель

Сделать каталог визуально ближе к DNS Shop, сохранив данные Electromagaz,
текущие маршруты, фильтры, корзину, сравнение и B2B-сценарий заявки.

## Референс и выводы

- Убрать табличную строку `Артикул / Наименование / Производитель / Срок / Цена`
  над карточками в списочном режиме.
- Карточка должна читаться тремя зонами: фото, характеристики, цена и действия.
- MPN показывать спокойной подписью под фото и усиливать при наведении.
- Кнопка покупки в покое нейтральная, при наведении на карточку становится синей.
- Белые карточки размещаются на светло-сером фоне, без прыгающих теней и лишнего декора.
- Desktop-шапка остается двухуровневой: сервисные ссылки сверху, бренд, каталог,
  поиск и действия во второй строке.
- Актуальная главная DNS начинается не с огромного hero, а с горизонтальной полки
  компактных сервисных карточек. Для Electromagaz повторяем эту композицию через
  B2B-сценарии: спецификация, MPN, аналоги, документация и серийные поставки.
- Над выдачей каталога нужны быстрые категории, а в боковой колонке - инженерный
  помощник и поиск по дереву категорий.

## Публичные контракты

- [x] Сохранить query-параметры `/catalog`: `q`, `category`, `manufacturer`, `sort`,
  `view`, `limit`, `page`.
- [x] Сохранить `electromagaz_cart`, сравнение, recently viewed и `data-cart-icon`.
- [x] Не менять Server Actions, формы заявки, цены и SEO/JSON-LD.
- [x] Не использовать изображения и фирменные материалы DNS.

## Фазы

- [x] Проверить Git, текущий код, пользовательские скриншоты и сохраненный DNS-аудит.
- [x] Переделать list-card и hover-состояния каталога.
- [x] Уплотнить desktop-шапку и улучшить первый экран главной.
- [x] Проверить unit, lint, TypeScript и production build.
- [x] Проверить desktop/mobile в браузере, включая hover, корзину и отсутствие overflow.
- [x] Зафиксировать результат и обновить существующий draft PR.
- [x] Получить визуальное подтверждение владельца, merge и проверить Production.

## Результат реализации

- Удалена псевдотабличная строка над карточками list-view.
- MPN появляется под фото на desktop hover и остается доступен в mobile-контенте.
- Кнопка корзины получает главный синий акцент при наведении на карточку.
- Коммерческий блок показывает наличие или поставку под заказ и честный статус срока.
- Desktop-шапка стала компактнее, поиск сохранил приоритет по ширине.
- Вместо большого hero главная получила актуальную DNS-подобную сервисную полку с
  горизонтальным скроллом на mobile.
- В каталоге появились функциональные быстрые категории, инженерный helper и поиск
  по дереву категорий.
- Компактная guide-полка над товарами приведена к актуальной композиции DNS.
- Действие сравнения перенесено к характеристикам и получило видимую подпись.
- После просмотра усилена насыщенность pastel-карточек, изображений и быстрых
  фильтров на один шаг без изменения нейтрального фона и товарных карточек.
- Добавлен E2E-контракт для структуры list-view и hover-состояний.
- Обновлены и просмотрены desktop/mobile baseline для `/` и `/catalog`; прежние
  контракты `/cart` и `/compare` сохранены и прошли полный E2E-набор.
- Владелец подтвердил публикацию, PR #11 слит merge-коммитом `fd042cf` в `main`.
- Vercel Production deployment `dpl_y6TViwh3DSJDvsGJsdBdZDX3vxMX` получил `Ready`
  и назначен на `https://magazik-94yr.vercel.app`.
- Production smoke подтвердил 200 для `/`, `/best`, `/catalog` и
  `/api/catalog/categories`; каталог отдал 50 строк, API - 8 корневых категорий.
- Runtime release зафиксирован в `main`: feature `770416a`, merge `fd042cf`;
  последующие изменения второго мозга не меняют storefront-код.

## Стоп-условие

Стоп-условие выполнено: владелец просмотрел результат и явно распорядился слить его
в `main`; Production проверен после статуса `Ready`.

## Точка продолжения

- Текущая задача завершена, незакоммиченных storefront-изменений нет.
- Первый продуктовый хвост - принять решение по пустому `/best`: данные
  `priceWholesale`, fallback или empty state.
- Первый инфраструктурный хвост - не чинить Preview боевой БД; при необходимости
  создать отдельную Supabase branch или sandbox.
- Новую работу начинать от свежего `main` в отдельной ветке.
