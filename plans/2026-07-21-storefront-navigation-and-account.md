# Упрощение витрины, каталога и возврат личного кабинета

Дата: 2026-07-21
Статус: активен, текущий этап отправлен как промежуточная контрольная точка
Ветка: `codex/dns-storefront-redesign`
Remote HEAD: `6abcc0f`

Важно: push от 2026-07-21 не закрывает редизайн. Владелец продолжает визуальную
приемку и будет передавать следующие правки отдельными итерациями.

## Решение владельца

- Убрать с главной статические товарные полки `Недавно добавили` и
  `Чаще всего заказывают`.
- Оставить только реальные недавно просмотренные товары.
- Остановить бегущую строку производителей и показывать бренды статично.
- Полностью удалить всплывающее мегаменю каталога.
- Кнопка `Каталог` всегда ведет на `/catalog`.
- `/catalog` сразу показывает рабочий листинг с деревом категорий и товарами.
- Заменить действие `Бренды` в основной шапке на `Профиль`.
- Вернуть страницу входа и регистрации, не выдавая frontend-мок за реальную auth.
- Не показывать отдельное упоминание `Честного ЗНАКА` на главной.
- Сделать изображения главной ярче и убрать блок поиска по точному MPN.
- Увеличить hero и фотомодули тематической ленты.
- Показывать бренды одной длинной статичной строкой без автопрокрутки.

## Сохраняемые контракты

- Query-параметры `/catalog`: `q`, `category`, `manufacturer`, `sort`, `view`,
  `limit`, `page`.
- Корзина, сравнение и `data-cart-icon`.
- `electromagaz:recently-viewed` до отдельной миграции хранения.
- Server Actions заявок и текущая схема БД.
- Metadata, canonical, robots, sitemap и JSON-LD.

## Текущая реализация `[x]`

- [x] Удалить товарные полки и лишний MPN-блок с главной.
- [x] Сделать бренды одной статичной строкой с ручным mobile-скроллом.
- [x] Осветлить hero и категорийные фотографии без замены исходных файлов.
- [x] Убрать карточку и упоминание `Честного ЗНАКА` с главной.
- [x] Увеличить hero и шесть фотомодулей тематической ленты.
- [x] Удалить состояние, fetch и DOM мегаменю из `StickyNav`.
- [x] Перевести desktop/mobile кнопку каталога на обычную ссылку `/catalog`.
- [x] Удалить корневую витрину категорий `/catalog` и всегда рендерить листинг.
- [x] Заменить действие `Бренды` на `Профиль`.
- [x] Создать `/account` с формами входа и регистрации и честным сообщением о
  неподключенной серверной авторизации.

## Этап 2: реальные недавно просмотренные `[ ]`

- Хранить в подписанной cookie только идентификаторы или slug последних товаров,
  без названий, цен и другого дублирующего payload.
- Ограничить список 8-12 позициями и размер cookie, задать `SameSite=Lax`,
  `Secure` и срок жизни.
- Читать cookie на сервере и получать актуальные карточки одним batch-запросом.
- Кэшировать неизменяемые данные карточек и не дублировать запросы главной.
- Добавить согласие на необязательные cookies, если хранение будет использоваться
  не только для строго функционального сценария.

## Этап 3: реальная авторизация и профиль `[ ]`

- Выбрать auth-провайдера и описать модель `User`, `Session`, `Order`,
  `CustomerMessage`, `Address` и согласия на обработку ПДн.
- Реализовать регистрацию по российскому email или телефону.
- Подключить реальные одноразовые коды через email/SMS-провайдера, rate limit,
  срок действия кода и блокировку перебора.
- Хранить профиль и сессии в БД, пароли самостоятельно не хранить.
- В личном кабинете показать реальные статусы заказов, системные сообщения,
  адреса, реквизиты и настройки уведомлений.
- Добавить аудит входов, отзыв сессий и удаление аккаунта.

## Проверка `[x]`

- [x] `npm exec tsc -- --noEmit`.
- [x] `npm run lint` - 0 ошибок, 11 существующих предупреждений вне редизайна.
- [x] `npm test -- --run` - 199 тестов.
- [x] `npm run build`.
- [x] Desktop/mobile: главная, `/catalog`, `/account`.
- [x] Проверить отсутствие мегаменю, лишних товарных полок и горизонтального overflow.
- [x] Проверить консоль браузера - 0 ошибок.
- [x] `git diff --check`.

## Стоп-условие

Текущий проход завершен, когда семь правок со скриншотов подтверждены в браузере,
а формы аккаунта не сохраняют и не отправляют персональные данные до подключения
реальной серверной авторизации. Это стоп-условие только текущего прохода, а не
всего DNS-подобного редизайна.
