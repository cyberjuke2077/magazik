# Watermark removal: Florence-2 (detection) + LaMa (inpainting).
# Устанавливать в venv с Python 3.11 (uv venv --python 3.11).
#
# LaMa берём через simple-lama-inpainting (лёгкая обёртка, без жёстких
# version-pins, в отличие от iopaint — у того pillow==9.5.0 конфликтует
# с Florence-2/transformers).
#
# На сервере с CUDA поставить CUDA-сборку torch ДО этого файла:
#   uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# pillow/numpy НЕ пиним — их версии диктует simple-lama-inpainting
# (pillow<10). transformers/Florence-2 с pillow 9.5 совместимы.
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.44.0
simple-lama-inpainting>=0.1.2
einops>=0.8.0
timm>=1.0.0
# для R2-обёртки clean_r2_watermarks.py (доступ к БД + R2)
psycopg[binary]>=3.1.0
boto3>=1.34.0
python-dotenv>=1.0.0
requests>=2.31.0
