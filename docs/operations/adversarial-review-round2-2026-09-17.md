# Повторное адверсариальное ревью - 2026-09-17

## Вывод и область проверки

Найдены 14 дефектов и ограничений, описанных ниже. В первую очередь нужно исправить жизненный цикл заявки и передачу товара из каталога в корзину. Защита от повторов работает для неизменённого повторного запроса, но подавляет новый одинаковый заказ и теряет свою гарантию при редактировании формы после неоднозначного результата. Предыдущее закрытие расчётов и восстановления формы оказалось неполным.

Проверенный commit: `1d71451251f665b24e68844f2e403670a0fc159b`, ветка `codex/adversarial-review-20260917`. Перед ревью рабочее дерево было чистым. Это проверка локального кандидата, а не текущего production. Код приложения не менялся. На момент начала разговора GitHub main был проверен через `git ls-remote` и оставался на `2bd5d94480eb24e359a0bba10c232699ab5b88b5`; deployment SHA не проверялся.

Среда: Next.js 16.3.5 в режиме `next dev --webpack`, `http://127.0.0.1:3107`, отдельная PostgreSQL 16 `emg_audit2` на `127.0.0.1:55434`, контейнер `electromagaz-audit2-20260917`. Seed из `prisma/seed-local-mvp.ts`, только синтетические товары/заявки и тестовые контакты `example.invalid`. Telegram отключён. Production, существующие рабочие БД и почтовые клиенты не изменялись.

P1 - исправить перед выпуском покупательского сценария. P2 - существенный дефект или ограничение защиты, исправлять следующим циклом; условия эксплуатации указаны отдельно. P3 - устойчивость к повреждённому клиентскому состоянию. P0 и независимый обход аутентификации без знания сессии в этой проверке не обнаружены. Это не доказательство отсутствия других уязвимостей.

Выбор инфраструктуры по решению владельца отложен. Повторно выдавать его за новую находку этого ревью не нужно.

## R2-01. P1 - успешная заявка блокирует следующий одинаковый заказ

**Код:** `src/lib/submission-key.ts:4-18`, `src/lib/save-submission.ts:29-32`, `src/app/wholesale/page.tsx:113-116`, аналогичный вызов в `src/app/request-quote/page.tsx:117`.

**Воспроизведение:** отправить оптовую заявку, увидеть успех, обновить страницу в той же вкладке, снова заполнить те же поля и отправить. Оба раза показано «Заявка отправлена!», но число лидов с тестовым email осталось 1. Успешный ключ остаётся в sessionStorage; одинаковый payload получает прежний ключ, сервер возвращает старую квитанцию. У формы из корзины используется тот же механизм.

**Последствие:** повторный реальный заказ незаметно теряется, новое задание уведомления не создаётся. Хеш содержимого не определяет намерение покупателя.

**Исправление и приёмка:** отдельный ID операции/черновика; повтор неоднозначно завершившейся операции использует прежний ID, новая заявка после подтверждённого успеха получает новый. Две намеренные одинаковые заявки должны дать два номера; повтор потерянного ответа должен дать один.

## R2-02. P1 - добавление из каталога теряет оптовую цену и ломает ссылку товара

**Код:** `src/lib/request-list-store.ts:79-99`, `src/app/catalog/components/add-to-cart-btn.tsx:54-63`, `src/components/ui/cart-item.tsx:97`.

**Воспроизведение:** добавить TPS5430DDAR из каталога. В БД розница 430 ₽, опт 385 ₽, минимум 10. Каталог сохраняет объект без `priceWholesale`, со `slug`, равным ID. Первое добавление даёт 10 × 430 = 4 300 ₽ вместо оптовых 3 850 ₽ по текущему правилу `cartUnitPrice`. Нажатие названия в корзине ведёт на `/product/<id>` и показывает «Страница не найдена». После повторного добавления зафиксировано 20 × 430 = 8 600 ₽. При добавлении из самой карточки передаётся полный Product.

**Последствие:** цена одной партии зависит от места добавления; из корзины нельзя вернуться к документации выбранного товара. Табличный и массовый сценарии используют тот же адаптер.

**Контроль:** добавление того же товара основной кнопкой карточки дало валидный slug, оптовую цену 385 ₽ и итог 3 850 ₽ за 10 штук в отрисованной корзине.

**Исправление и приёмка:** единый контракт добавления по ID с актуальными данными или полный валидированный Product. Проверить каталог, таблицу, массовое добавление и карточку: одинаковая цена, валидный slug и открывающийся товар.

## R2-03. P2 - поле email позволяет внедрить параметры почтовой ссылки

**Код:** `src/lib/validate-wholesale-input.ts:12,38`, `src/lib/validate-quote-input.ts:17,52`, `src/app/admin/(panel)/wholesale/page.tsx:62`, `src/app/admin/(panel)/requests/[id]/page.tsx:103`.

**Воспроизведение:** прямой вызов настоящего публичного action с email `audit2-victim@example.invalid?bcc=audit2-copy%40example.invalid` принят, вернул success и сохранил лид. Админка отрисовала `mailto:audit2-victim@example.invalid?bcc=audit2-copy%40example.invalid`. URI содержит отдельный параметр `bcc`, декодируемый в `audit2-copy@example.invalid`. Нативный input type=email не защищает публичный action.

**Последствие:** при открытии ссылки поддерживающий этот параметр почтовый клиент может добавить постороннего получателя. Это подтверждённая инъекция параметров URI; реальная отправка или утечка письма не проверялись. Интерпретация заголовков зависит от почтового клиента: [RFC 6068, разделы 2, 4 и 7](https://www.rfc-editor.org/rfc/rfc6068).

**Исправление и приёмка:** строгий разбор единственного адреса на сервере, запрет query/fragment в поле адреса, корректное percent-encoding адреса при построении mailto. Данный payload должен отклоняться; адреса с допустимым плюсом должны работать. Не полагаться только на HTML escaping.

## R2-04. P2 - «Аналоги» не проверяют совместимость

**Код:** `src/app/product/[slug]/page.tsx:43`, `src/lib/queries/products.ts:146-154`, `src/app/product/[slug]/product-client-chipdip.tsx:472-488`.

**Воспроизведение:** в синтетическую категорию «Пассивные компоненты» к резистору RC0603FR-0710KL добавлен конденсатор 100 мкФ 50 В. Карточка резистора показывает этот конденсатор в блоке «Аналоги». Запрос фильтрует только категорию и исключает исходный ID; электрические параметры, тип и корпус не сопоставляются.

**Последствие:** покупатель получает ложный сигнал о взаимозаменяемости компонентов. Реальная установка или повреждение оборудования не проверялись.

**Исправление и приёмка:** до появления проверенных связей заменить название на «Другие товары категории». Настоящие аналоги требуют явного источника совместимости и условий замены; одной категории недостаточно.

## R2-05. P2 - ограничение входа доверяет подставленному X-Forwarded-For

**Код:** `src/lib/submission-rate-limit.ts:78-97`, `src/app/admin/login/actions.ts:22`.

**Воспроизведение:** 11 одинаковых запросов с фиксированным неверным паролем и одним `X-Forwarded-For` к локальному серверу: первый возвращает «Неверный логин», одиннадцатый блокируется лимитером. Замена только этого заголовка снова приводит к проверке пароля. У публичных форм тот же выбор сетевого идентификатора.

**Граница:** это воспроизведение риска из первого ревью на прямом Next.js ingress. Эксплуатация через Vercel не проверялась, его нормализацию заголовков здесь не утверждаем. Неверный пароль не дал доступ к админке.

**Исправление и приёмка:** определить доверенный ingress, перезаписывать клиентские forwarding-заголовки, не публиковать Next.js в обход прокси; добавить независимый лимит на административную учётную запись. Тестировать именно схему будущего размещения, сохраняя этот пункт в её критериях приёмки.

## R2-06. P2 - «Выйти» не отзывает административную сессию

**Код:** `src/app/admin/login/actions.ts:45-48`, `src/lib/admin-auth.ts:14,59-66`.

**Воспроизведение:** войти с синтетическими локальными доступами, сохранить cookie только в памяти теста, нажать «Выйти». Запрос без cookie получает 307 на login; запрос с прежней cookie получает 200 и панель управления. Токен действует до истечения недельного срока. Смена ADMIN_PASSWORD инвалидирует его, обычный logout - нет.

**Граница:** атакующему нужна ранее полученная валидная cookie; её кража и обход подписи не продемонстрированы. Это отсутствие отзыва сессии, а не вход с произвольной cookie.

**Исправление и приёмка:** серверная сессия или проверяемая версия/реестр отзыва, отзыв при logout, подходящий срок административной сессии. После выхода повтор той же cookie должен быть отвергнут.

## R2-07. P2 - редактирование после потерянного ответа создаёт дубль

**Код:** `src/lib/submission-key.ts:5,13-16`, `src/app/wholesale/page.tsx:120-121`.

**Воспроизведение:** тестовый браузер пропускает POST до сервера, затем теряет только ответ. Первая запись сохранена. Пользователь видит обещание «дубль заявки не создастся», меняет последнюю цифру телефона и повторяет. Новый хеш выдаёт новый ключ; в БД два лида с тем же email и спецификацией.

**Исправление и приёмка:** после неопределённого результата сначала разрешить статус исходной операции. Изменение payload для существующей операции требует явного сценария изменения/новой заявки, а не автоматического нового ключа. Проверить также изменение количества и комментария. Связано с R2-01, но имеет противоположное последствие.

## R2-08. P2 - каталог по-прежнему округляет копейки до нуля

**Код:** `src/lib/catalog-utils.ts:88-95`, импорт в `src/app/catalog/components/product-row.tsx:4` и `product-table.tsx:6`.

**Воспроизведение:** синтетическая цена резистора 0,24 ₽ отрисовалась в каталоге как «0 ₽». Исправленный `src/lib/utils.ts` не используется этими двумя представлениями каталога. Цена в БД после пробы восстановлена.

**Исправление и приёмка:** общий форматтер денег, тесты реальных представлений для 0,01 / 0,24 / 2,40 / 160 ₽ и неизвестной цены. Это неполное закрытие прежнего замечания о копейках.

## R2-09. P2 - корзина не обновляет цены и минимальные партии

**Код:** `src/hooks/use-cart.ts:10-17,33-47`, `src/lib/create-quote.ts:5-17`.

**Воспроизведение:** после добавления товара изменить его локально с цены 430 и minOrder 10 на 860 и minOrder 30. Обновить страницу корзины. В localStorage и интерфейсе остались прежние цена/минимум, количество 20. Сервер создания заявки сверяет новый минимум, но браузер не получает обновлённый товар даже при reload. Это даёт ошибку только на последнем шаге. Отдельно код сохранения заявки не сохраняет снимок показанной цены.

**Исправление и приёмка:** хранить идентификаторы/количества и подтягивать актуальные предложения; показывать изменения до отправки. Удалённый товар должен иметь понятный путь восстановления. Снимок предварительной цены полезен для разбирательств, но не должен трактоваться как подтверждённая оферта.

## R2-10. P2 - кнопка «В корзине» незаметно увеличивает количество

**Код:** `src/app/catalog/components/add-to-cart-btn.tsx:54-65,82-97`, `src/lib/request-list-store.ts:72-76`.

**Воспроизведение:** добавить 10 штук из каталога, нажать ту же кнопку с надписью «В корзине». В корзине уже 20, а счётчик рядом с кнопкой по-прежнему показывает 10. Обработчик повторно прибавляет количество и не синхронизирует локальный state с хранилищем.

**Исправление и приёмка:** после добавления кнопка открывает корзину или явно обозначает «Добавить ещё N». Счётчики должны показывать фактическое количество и обновляться при массовом добавлении, удалении и изменениях в другой вкладке.

## R2-11. P2 - главный сценарий отправки спецификации не попадает в «Ваши заявки»

**Код:** `src/app/wholesale/actions.ts:71`, `src/app/wholesale/page.tsx:115-116,246-254`, `src/app/account/account-client.tsx:22-25`.

**Воспроизведение:** после отправки через `/wholesale` виден только текст успеха. Номер не возвращается клиенту, история `electromagaz_request_history` остаётся пустой; страница «Ваши заявки» не знает об этом лиде. После reload нет и подтверждения. Основной CTA магазина ведёт именно сюда.

**Исправление и приёмка:** единая квитанция с ID и безопасной страницей статуса для обоих типов обращений; сохранение ссылки в истории. Отсутствие email уже известно и не считается отдельной новой находкой, но сейчас и локальный путь восстановления отсутствует.

## R2-12. P2 - обещанный сохранённый черновик исчезает после reload

**Код:** `src/app/wholesale/page.tsx:76-82,120-121`, `src/app/request-quote/page.tsx:25-36,129-130`.

**Воспроизведение:** заполнить контакты и BOM, оборвать запрос, получить «Данные формы сохранены», обновить страницу. Email и спецификация пустые. В памяти остаётся только состояние текущего React-компонента, а sessionStorage содержит лишь хеш/ключ отправки.

**Исправление и приёмка:** явно определить политику черновика, срок жизни и место хранения. Не сохранять контакты бессрочно без решения. Как минимум убрать ложное обещание, предупредить о выходе и дать сохранить спецификацию; при восстановлении черновика проверить reload/back/закрытие вкладки согласно выбранной политике.

## R2-13. P2 - поиск «по производителю» не ищет производителя в товарной выдаче

**Код:** `prisma/migrations/20260515172031_add_catalog_fts_and_quote_request/migration.sql:4-9`, `src/lib/queries/products.ts:275-283`, `src/components/ui/live-search-dropdown.tsx:167-174`.

**Воспроизведение:** в синтетическом каталоге есть TPS5430DDAR от Texas Instruments. `/catalog?q=Texas` показывает 0 позиций; `/catalog?manufacturer=texas-instruments` показывает 1. Поисковое поле обещает поиск по производителю, но Enter запускает FTS только по MPN, имени и описанию товара. Подсказка производителя и её отдельный фильтр работают иначе.

**Исправление и приёмка:** согласовать семантику поиска и подсказок, учитывать производителя или явно преобразовывать выбор производителя в фильтр. Проверить Enter и выбор подсказки для известного бренда.

## R2-14. P3 - валидный JSON неправильной формы ломает витрину

**Код:** `src/hooks/use-cart.ts:14,103`, `src/lib/request-list-store.ts:32`.

**Воспроизведение:** в тестовом браузере установить `electromagaz_cart` в строку `null`, обновить страницу. Вместо корзины и шапки появляется общий error boundary «Не удалось загрузить страницу»; header отсутствует. try/catch вокруг JSON.parse не проверяет схему возвращённого значения. Исходное содержимое восстановлено.

**Граница:** для этой пробы состояние менялось вручную; удалённый способ записать это значение покупателю не показан. Это устойчивость к повреждению/миграции хранилища, не XSS и не атака на чужие браузеры.

**Исправление и приёмка:** runtime-проверка массива и каждой позиции, версия схемы, восстановление без падения общей шапки. Проверить null, объект вместо массива и неполный Product.

## Статус локальных исправлений

Исходные доказательства выше описывают состояние проверенного commit
`1d71451251f665b24e68844f2e403670a0fc159b`. Исправления распределены по commits
`08a6e643898cce9ac27b5ec8f941b5ffeb70e82f`,
`bf3616fed16f9d510dbf725accb99eed82ca934b` и
`5316955269b76b72cebbf87c0560863923053115`; последний является вершиной кодовой
части кандидата в ветке `codex/adversarial-review-20260917`. Production не
менялся. Финальный adversarial diff-review не нашёл новых P0-P2.

- R2-01 - локально исправлено. Подтверждённый успех завершает операцию, поэтому
  следующая намеренная заявка получает новый ID. Неоднозначный ответ сохраняет
  прежний ID и снимок исходного payload для безопасного повтора.
- R2-02 - локально исправлено. Каталог, таблица и массовое добавление передают
  канонический снимок товара с корректными slug, оптовой ценой и minOrder.
- R2-03 - локально исправлено. Сервер принимает только один допустимый email без
  query/fragment, а административные `mailto:` строятся через безопасный helper.
- R2-04 - локально исправлено. Непроверенный блок переименован в «Другие товары
  категории» и больше не обещает совместимость.
- R2-05 - локально усилено, production acceptance остаётся открытым. Вход в
  админку ограничен последовательными глобальным, сетевым и account-лимитерами
  с ограниченной кардинальностью ключей. Перед выпуском нужно на реальном ingress
  подтвердить перезапись forwarding-заголовков и отсутствие прямого обхода
  доверенного прокси.
- R2-06 - локально исправлено. Административная cookie ссылается на серверную
  `AdminSession`, logout отзывает запись, а повтор старой cookie отклоняется.
- R2-07 - локально исправлено. После потерянного ответа сначала сверяется статус
  исходной операции; изменение формы не создаёт новую заявку автоматически.
- R2-08 - локально исправлено. Каталог использует единый денежный форматтер,
  который сохраняет копейки и не превращает цену меньше рубля в ноль.
- R2-09 - локально исправлено. Корзина сверяет товары с сервером, обновляет цену
  и minOrder до отправки и явно сообщает об изменённых или удалённых позициях.
- R2-10 - локально исправлено. Состояние «В корзине» открывает корзину, а счётчик
  количества синхронизируется с фактическим содержимым.
- R2-11 - локально исправлено. Оптовая форма возвращает номер, сохраняет запись
  в истории и ведёт на безопасную страницу статуса без контактов и спецификации.
- R2-12 - локально исправлено. Черновик и снимок операции хранятся в
  `sessionStorage` до двух часов; согласие не восстанавливается, политика описана
  на странице конфиденциальности.
- R2-13 - локально исправлено. Товарная выдача учитывает имя производителя через
  параметризованный и экранированный `ILIKE`.
- R2-14 - локально исправлено. Хранилище корзины версионировано, все поля проходят
  runtime-проверку, повреждённое или устаревшее состояние восстанавливается без
  падения витрины.

Дополнительно финальный проход закрыл сетевой attempt-limit публичных форм до
валидации и lookup без общего app-wide лимита, стабильный tie-breaker поиска,
стабильный fingerprint отправляемой корзины, защиту от двойного submit, route
metadata для клиентских переходов и дату поставки по московскому календарю с
повторной серверной проверкой.

## Что проверено и выдержало пробы

- Свежий `npm audit --json`: 0 известных уязвимостей во всех проверенных зависимостях. Это не аудит собственной логики.
- Unit: 49 файлов, 388 тестов, все прошли.
- Readiness integration: 1 файл, 9 тестов, все прошли.
- Targeted E2E: 17 passed, 1 ожидаемо skipped для desktop-only проверки.
- Полный функциональный E2E: 63 passed, 1 ожидаемо skipped.
- `npx tsc --noEmit --incremental false`, ESLint, `git diff --check` и production
  build через `npm run build -- --webpack` прошли.
- Все 12 миграций применились к чистой изолированной PostgreSQL. Runtime-роль
  осталась без LOGIN, superuser, CREATEDB, CREATEROLE, REPLICATION и BYPASSRLS,
  без membership и владения объектами схемы `public`; разрешённый CRUD и запрет
  `EnrichmentJournal` проверены отдельными пробами.
- Browser smoke подтвердил поиск по производителю, оптовую цену 3 850 ₽ за 10
  единиц, переход в корзину, клиентские title и minimum даты поставки.
- Визуальный baseline не принят: эталонные снимки используют другой fixture
  каталога, а финальный E2E работал на трёх детерминированных seed-товарах.
- Публичный cron без секрета отвечает 401; `/api/admin/example` без сессии - 401; `/admin/requests` - redirect 307 на login.
- Межсайтовый POST с чужим Origin отвергнут, тестовая запись не появилась. В dev Next вернул 500 для этого запрещённого действия; это не основание утверждать успешный CSRF.
- На странице статуса присутствуют `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer`, `X-Robots-Tag: noindex, nofollow, noarchive`.
- В проверенных server actions админки есть собственная проверка сессии. В поисковых SQL используются параметры; подтверждённой SQL injection не найдено.
- Mobile 390 px: каталог, корзина, checkout, спецификация и история не дали горизонтального переполнения в проверенном состоянии. Это не полный аудит доступности.
- Нет утверждения о новых CVE, удалённом захвате админки, взломе Supabase или реальной утечке писем.

## Чего не хватает мне как покупателю

1. **Подтверждения вне текущего браузера:** email с номером заявки и подтверждённый срок первого ответа. Локальная ссылка и история уже работают, но не заменяют канал связи.
2. **Проверяемого состава КП:** скачать или открыть документ с позициями, количеством, ценой, сроком действия и условиями поставки. Текущая страница статуса намеренно не раскрывает состав по одной ссылке.
3. **Работы со спецификацией:** загрузка CSV/XLSX или вставка таблицы с разбором MPN/количеств, показом нераспознанных строк и разрешённых замен. Сейчас есть только свободный текст; безопасный файловый импорт требует отдельной реализации, нельзя просто публиковать локальный importer.
4. **Проверенных технических данных:** документ производителя, происхождение/актуальность характеристик, подтверждённые аналоги. Пустые или неподтверждённые сведения не должны имитировать совместимость.
5. **Понятного продолжения после «КП готово»:** задать вопрос по конкретной заявке, уточнить или отменить её. Сейчас статус предлагает самостоятельно связаться с менеджером.
6. **Однозначного B2B-позиционирования:** компания/ИНН обязательны в checkout. Если розница не входит в продукт, обещания «в розницу» не должны приводить физлицо в тупик. Решение о поддержке розницы не принято в этом ревью.

Это предложения по результатам пути покупателя, а не согласованный scope разработки. Онлайн-оплата и полноценная регистрация не объявляются обязательными для B2B-заявки на КП.

## Следующая работа и границы

Локальная реализация завершена. Push, PR, production migration, `db:publish`,
публикация каталога и deploy не выполнялись.

До production acceptance остаются обязательными отдельные проверки:

- применить миграции `20260917021149_submission_outbox`,
  `20260917230000_add_admin_sessions` и
  `20260917233000_ensure_runtime_role_grants` к точному Supabase target;
- под runtime-ролью проверить RLS и GRANT на известных строках, включая запреты;
- на Vercel подтвердить trusted ingress, перезапись forwarding headers и
  отсутствие прямого обхода Next.js;
- проверить scheduler, реальное Telegram/email уведомление и backup/restore;
- подтвердить регион хранения R2/PII и заполнить юридические реквизиты без
  плейсхолдеров.

Первое следующее действие - review локальных commits. После review владелец
отдельно решает, разрешать ли push/PR и начинать production acceptance.

Локальные доказательства исходного ревью вне Git: `/private/tmp/emg-audit2-evidence/`,
`/private/tmp/emg-audit2-unit.log`, `/private/tmp/emg-audit2-npm-audit.json`.
Постоянные регрессионные сценарии перенесены в unit, integration и E2E tests;
временные скрипты и локальные доступы не коммитились.

Локальные серверы и браузерная сессия остановлены. Контейнеры
`electromagaz-audit2-20260917` и `electromagaz-security-20260917` остановлены;
чужие контейнеры не затрагивались.
